# URL du backend (figée au build Vite — obligatoire pour l’APK)
# Production / téléphone réel (recommandé) :
VITE_API_URL=https://chatbot-production-5202.up.railway.app

# Émulateur Android Studio + backend local sur le PC :
# VITE_API_URL=http://10.0.2.2:8001

# Téléphone physique + backend sur le même Wi-Fi (remplacer par l’IP du PC) :
# VITE_API_URL=http://192.168.1.42:8001

# Dev web uniquement : laisser vide — le proxy Vite redirige /api vers :8001
