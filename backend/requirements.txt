# Les dépendances sont centralisées dans le requirements.txt à la racine du
# projet afin d'éviter toute duplication. Installez-les avec :
#   pip install -r ../requirements.txt   (depuis backend/)
#   pip install -r requirements.txt      (depuis la racine)
-r ../requirements.txt
